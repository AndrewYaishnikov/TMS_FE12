import React from "react";
import ReactDOM from 'react-dom';

import CardHolder from "./Components/CardHolder/CardHolder";

ReactDOM.render(<CardHolder/>, document.getElementById("root"));

//const taskList = [{taskName: "bRUSH TEETH", isDone: false}, {taskName: "bRUSH TEETH", isDone: false}];

//const rootDOMElement = document.getElementById("root");

//const cardList = taskList.map((task, index) => {return (new Card(task.taskName, task.isDone, rootDOMElement))});

//ReactDOM.render(React.createElement("div", null, React.createElement("div", null, "Test 1")), rootDOMElement);


